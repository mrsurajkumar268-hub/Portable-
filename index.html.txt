<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Suraj Kumar | Computer Professional Portfolio</title>
<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 30px 20px;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    
    /* TECH DOT GRID BACKGROUND SETUP */
    background-color: #030712;
    background-image: 
        radial-gradient(#1e3a8a 1px, transparent 1px), 
        radial-gradient(#1e293b 1px, transparent 1px);
    background-size: 24px 24px;
    background-position: 0 0, 12px 12px;
    position: relative;
}

body::before {
    content: "";
    position: absolute;
    inset: 0;
    background: 
        radial-gradient(circle at 15% 20%, rgba(30, 64, 175, 0.35) 0%, transparent 40%),
        radial-gradient(circle at 85% 85%, rgba(180, 83, 9, 0.2) 0%, transparent 45%);
    pointer-events: none;
    z-index: 0;
}

/* ===== ULTRA PREMIUM ROYAL METALLIC FRAME ===== */
.frame-container {
    position: relative;
    width: 100%;
    max-width: 440px;
    padding: 12px; 
    background: linear-gradient(135deg, #cbd5e1, #1e40af, #d4af37, #cbd5e1);
    border-radius: 36px;
    box-shadow: 
        0 30px 70px -15px rgba(0, 0, 0, 0.8),
        0 0 40px rgba(30, 64, 175, 0.25);
    z-index: 1;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.frame-container:hover {
    transform: translateY(-5px);
}

/* Inner Core Content Deck Base */
.profile-card {
    position: relative;
    width: 100%;
    padding: 45px 30px 30px;
    text-align: center;
    background: #ffffff;
    border-radius: 26px;
    overflow: hidden;
}

/* Geometric Boundary Layout Overlay Line */
.profile-card::after {
    content: "";
    position: absolute;
    inset: 12px;
    border: 1px solid rgba(226, 232, 240, 0.9);
    border-radius: 18px;
    pointer-events: none;
}

/* VERIFIED EXCLUSIVE BADGE */
.verified-tag {
    position: absolute;
    top: 24px;
    right: 24px;
    display: flex;
    align-items: center;
    gap: 4px;
    background: #f0fdf4;
    color: #16a34a;
    font-size: 10px;
    font-weight: 800;
    padding: 4px 10px;
    border-radius: 20px;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    border: 1px solid #dcfce7;
    z-index: 10;
}

/* PHOTO RENDER PORTRAIT BLOCK CONTAINER */
.photo-frame-wrapper {
    position: relative;
    width: 155px;
    height: 155px;
    margin: 10px auto 24px;
    padding: 6px;
    background: linear-gradient(135deg, #1e40af, #d4af37);
    border-radius: 32px; 
    box-shadow: 0 12px 28px -6px rgba(30, 64, 175, 0.35);
}

.photo-frame-inner {
    width: 100%;
    height: 100%;
    border-radius: 26px;
    overflow: hidden;
    background: #ffffff;
    border: 3px solid #ffffff;
}

.photo-frame-inner img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 15%; 
    transition: transform 0.4s ease;
}

.frame-container:hover .photo-frame-inner img {
    transform: scale(1.06);
}

/* TEXT ARCHITECTURE DECK */
.name {
    font-size: 28px;
    font-weight: 800;
    color: #111827;
    letter-spacing: -0.5px;
}

.designation {
    margin-top: 4px;
    color: #2563eb;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 2.5px;
    text-transform: uppercase;
}

.brand-slogan {
    margin-top: 10px;
    font-size: 12px;
    font-weight: 600;
    color: #64748b;
    font-style: italic;
    line-height: 1.4;
}

.divider {
    width: 45px;
    height: 3px;
    margin: 18px auto 22px;
    border-radius: 2px;
    background: linear-gradient(90deg, #1e40af, #d4af37);
}

/* INFORMATION INTERFACE */
.info-deck {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-bottom: 24px;
    position: relative;
    z-index: 5;
}

.info-row {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    text-align: left;
    background: #f8fafc;
    border: 1px solid #f1f5f9;
    border-radius: 14px;
    transition: all 0.25s ease;
}

.info-row:hover {
    background: #ffffff;
    border-color: #cbd5e1;
    transform: translateX(4px);
    box-shadow: 0 10px 20px -8px rgba(0, 0, 0, 0.05);
}

.icon-holder {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 38px;
    height: 38px;
    color: #ffffff;
    background: linear-gradient(135deg, #1e40af, #2563eb);
    border-radius: 10px;
    font-size: 16px;
    flex-shrink: 0;
    box-shadow: 0 4px 10px rgba(37, 99, 235, 0.2);
}

.text-details {
    display: flex;
    flex-direction: column;
    gap: 2px;
}

.label-title {
    color: #64748b;
    font-size: 9px;
    font-weight: 750;
    letter-spacing: 0.6px;
    text-transform: uppercase;
}

.value-text {
    color: #334155;
    font-size: 14px;
    font-weight: 650;
}

.highlight-blue {
    color: #1e40af;
    font-weight: 800;
}

/* MODERN TECH SKILL CHIPS */
.skills-badge-box {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 6px;
}

.skills-badge-box span {
    font-size: 11px;
    font-weight: 700;
    color: #1e40af;
    background: #eff6ff;
    padding: 4px 10px;
    border-radius: 6px;
    border: 1px solid #bfdbfe;
}

/* INTERACTIVE QUICK CONNECTOR UTILITIES */
.action-grid {
    display: flex;
    gap: 10px;
    position: relative;
    z-index: 5;
    margin-bottom: 24px;
}

.action-grid a {
    flex: 1;
    padding: 12px 6px;
    text-align: center;
    text-decoration: none;
    font-size: 13px;
    font-weight: 700;
    border-radius: 12px;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 4px;
}

.btn-primary-blue {
    background: linear-gradient(135deg, #1e40af, #1d4ed8);
    color: #ffffff;
    box-shadow: 0 6px 16px rgba(30, 64, 175, 0.25);
}

.btn-primary-blue:hover {
    transform: translateY(-2px);
}

.btn-whatsapp {
    background: #25d366;
    color: #ffffff;
    box-shadow: 0 6px 16px rgba(37, 211, 102, 0.25);
}

.btn-whatsapp:hover {
    background: #22c35e;
    transform: translateY(-2px);
}

.btn-secondary-white {
    background: #ffffff;
    color: #334155;
    border: 1px solid #cbd5e1;
}

.btn-secondary-white:hover {
    background: #f8fafc;
    border-color: #94a3b8;
    transform: translateY(-2px);
}

/* MAP MODULE */
.map-embed-wrapper {
    margin-top: 15px;
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid #e2e8f0;
    position: relative;
    z-index: 5;
}

.map-embed-wrapper iframe {
    width: 100%;
    height: 140px;
    border: 0;
    display: block;
}

.card-footer {
    margin-top: 25px;
    padding-top: 16px;
    border-top: 1px solid #f1f5f9;
    color: #94a3b8;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.5px;
}

@media (max-width: 420px) {
    .profile-card {
        padding: 40px 18px 24px;
    }
    .action-grid {
        flex-direction: column;
        gap: 10px;
    }
    .action-grid a {
        padding: 13px;
    }
}
</style>
</head>
<body>

<div class="frame-container">
    <main class="profile-card">
        <!-- THE VERIFIED LOGO TAG -->
        <div class="verified-tag">
            <span>✓</span> Verified
        </div>

        <!-- METALLIC RING PHOTO FRAME -->
        <div class="photo-frame-wrapper">
            <div class="photo-frame-inner">
                <img src="C:\Users\rinku\Pictures\59ee47b1-fa61-4c24-92c1-637708e095c6.jfif">
            </div>
        </div>

        <!-- CORE IDENTITY DATA -->
        <h1 class="name">Suraj Kumar</h1>
        <p class="designation">Computer Professional</p>
        <p class="brand-slogan">Empowering Innovation Through Code & System Architecture</p>
        
        <div class="divider"></div>

        <!-- DESCRIPTIVE RECORDS -->
        <div class="info-deck">
            <div class="info-row">
                <div class="icon-holder">🏢</div>
                <div class="text-details">
                    <span class="label-title">Affiliation</span>
                    <span class="value-text highlight-blue">S.S.R. COMPUTER CENTER</span>
                </div>
            </div>

            <div class="info-row">
                <div class="icon-holder">🎓</div>
                <div class="text-details">
                    <span class="label-title">Education & Credentials</span>
                    <span class="value-text">B.A. Pass | NIELIT 'O' & 'A' Level</span>
                </div>
            </div>

            <div class="info-row">
                <div class="icon-holder">📍</div>
                <div class="text-details">
                    <span class="label-title">Official Address</span>
                    <span class="value-text">Pandari Bazar, Mirzapur</span>
                </div>
            </div>

            <div class="info-row">
                <div class="icon-holder">💻</div>
                <div class="text-details">
                    <span class="label-title">Technical Expertise</span>
                    <div class="skills-badge-box">
                        <span>CCC</span>
                        <span>'O' Level</span>
                        <span>'A' Level</span>
                        <span>Tally Prime</span>
                        <span>Python</span>
                        <span>Web Design</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- INTERACTIVE QUICK ROUTING CONNECTORS -->
        <div class="action-grid">
            <a href="tel:+919876543210" class="btn-primary-blue">📞 Call</a>
            <a href="https://wa.me." class="btn-whatsapp" target="_blank">💬 WhatsApp</a>
            <a href="mailto:suraj@example.com" class="btn-secondary-white">✉️ Email</a>
        </div>